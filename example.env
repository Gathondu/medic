NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=clerk-perishable-key
CLERK_SECRET_KEY=clerk-secret-key
BACKEND_URL=http://127.0.0.1:8000
# Optional: required for `next dev` so the browser calls FastAPI (not Next). Omit for Docker/static (same-origin).
NEXT_PUBLIC_API_ORIGIN=http://127.0.0.1:8000
